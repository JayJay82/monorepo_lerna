{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.10.7"
  },
  "scripts": {
    "update-version": "lerna publish  --no-git-tag-version --skip-npm",
    "clean": "lerna run clean",
    "check-packages": "npm run clean"
  }
}
